- [x] Inspect backend package structure and existing routers
- [x] Recreate `interview-prep-platform/backend/main.py` with a valid FastAPI `app` object and router includes
- [x] Ensure imports work when running `uvicorn backend.main:app`

- [x] Verify ASGI import works via `python3 -c` (import app)

- [x] (Optional) provide correct `uvicorn` command for running the API



